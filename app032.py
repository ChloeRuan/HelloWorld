# build modules to recognize codes
import converters

"""
second way to do it 
from converters import  kg_to_lbs
kg_to_lbs(100)
"""

print(converters.kg_to_lbs(70))

# Exercise


from utils import find_max

numbers = [10, 3, 6, 2]
maximum = find_max(numbers)
print(maximum)  # max has the same color as teh print, it suggests that this is a built-in function in python
